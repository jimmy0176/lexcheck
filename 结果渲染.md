请对“快速体检报告生成模块”进行结构化升级，目标是实现：

【目标】
1. AI输出“可解析表格结构”
2. 后端解析表格
3. 使用程序生成Word表格（而不是纯文本）

---

【第一步：定位现有代码】

请先找到：
1. 快速体检报告生成入口
2. prompt.md / output.md 的读取位置
3. Word 导出逻辑（python-docx 或其他）

先给我当前调用链路说明

---

【第二步：实现表格解析模块】

新增函数：

parseTableFromText(text)

功能：
- 输入：AI返回的文本
- 输出：二维数组（table）

规则：

1. 识别包含 "|" 的行
2. 按换行拆分
3. 每行按 "|" split
4. trim 空格
5. 过滤空行

示例输出：

[
  ["风险类型","风险描述","风险等级","建议"],
  ["人力资源","制度不完善","中","完善制度"]
]

---

【第三步：替换Word生成逻辑】

当前如果是：

doc.add_paragraph("风险类型 | 风险描述 ...")

请改为：

1. 识别这是表格段
2. 使用 python-docx：

table = doc.add_table(rows, cols)

3. 填充数据：

table.cell(i,j).text = value

---

【第四步：识别表格区块】

在生成报告时：

如果检测到：

连续多行包含 "|"

则：
→ 判定为表格区块
→ 使用表格渲染

否则：
→ 作为普通段落

---

【第五步：增强日志（必须）】

输出：

- 检测到表格数量
- 每个表格行数
- 是否成功解析

---

【第六步：保持兼容】

要求：

- 不影响原有文本输出
- 表格失败时 fallback 为文本

---

【第七步：验收标准】

1. Word中出现真正表格（不是文本）
2. 表格列对齐
3. 至少2个表格成功渲染
4. 不再出现“|”字符展示在最终Word中
5. 报告整体更接近正式法律文档

---

完成后请输出：

1. 修改文件列表
2. 表格解析逻辑说明
3. 示例输入输出
4. 测试方法